generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id       String @id @default(uuid())
  name     String
  email    String @unique
  password String
}

model Course {
  id        String   @id @default(cuid())
  title     String
  createdAt DateTime @default(now())
  icon      String
}

model Post {
  id        String       @id @default(cuid())
  slug      String       @unique
  category  CategoryPost
  content   String       @db.Text
  url       String?
  createdAt DateTime     @default(now())
  imagesUrl String[]
}

model Newsletter {
  id        String             @id @default(cuid())
  email     String             @unique
  category  CategoryNewsletter
  createdAt DateTime           @default(now())
}

model ErasmusCourse {
  id          String   @id @default(cuid())
  title       String
  description String
  pdf         String?
  url         String
  createdAt   DateTime @default(now())
  imageUrl    String
}

model ErasmusProject {
  id        String   @id @default(cuid())
  title     String
  url       String   @unique
  createdAt DateTime @default(now())
  imageUrl  String
}

model Partner {
  id        String   @id @default(cuid())
  name      String
  url       String   @unique
  createdAt DateTime @default(now())
  imageUrl  String
}

model Video {
  id        String   @id @default(cuid())
  title     String
  url       String   @unique
  createdAt DateTime @default(now())
  imageUrl  String   @unique
}

enum CategoryPost {
  Web
  National
  International
}

enum CategoryNewsletter {
  National
  International
}
